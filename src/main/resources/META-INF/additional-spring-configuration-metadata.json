{
  "properties": [
    {
      "name": "jwt.secret",
      "type": "java.lang.String",
      "description": "JWT HS256 签名密钥（至少 32 字节），可通过环境变量 JWT_SECRET 覆盖。"
    },
    {
      "name": "jwt.expiration",
      "type": "java.lang.Long",
      "description": "JWT 令牌有效期（秒）。",
      "defaultValue": 2592000
    }
  ]
}
