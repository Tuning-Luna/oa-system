# 测试账号说明

> 用于本 OA 系统本地开发/联调。登录页也内置了 admin 提示（`frontend/src/views/login/index.vue`，无上线需求，如需对外发布请移除）。

## 有效账号（当前数据库仅剩这 2 个）

| 账号 | 密码 | 角色 | 说明 |
|---|---|---|---|
| `admin` | `Admin@123456` | `admin` | 系统管理员，拥有全部权限（用户/角色/菜单管理 + 审批等） |
| `f2test` | `Passw0rd123` | 无 | 普通员工，用于测试「无角色」体验、提交请假/报销（可指定 admin 审批） |

## 登录与权限

- 地址：前端 `http://localhost:5173`，后端 `http://localhost:8080`（Swagger：`/swagger-ui.html`）
- 请求头认证：`Authorization: Bearer <token>`（token 有效期 30 天）
- 注册的新用户默认无角色，需用 admin 在「系统管理 → 用户管理 → 分配角色」后才有权限

## 备注

- 后端自动化测试套件统一用 `Passw0rd123` 作为测试账号密码（历史遗留，已清理，仅供参考）。
- 密码规则：8-20 位，须同时包含字母和数字。
